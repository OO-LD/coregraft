# coregraft-example

A coregraft example project

## Schemas

| Schema | Description |
| --- | --- |
| [Person](schemas/Person.schema.json) | An example schema; replace it with your own. |

Every released version serves its schemas under `/<version>/schemas/`, so a `$ref` or `@context` can point at an immutable URL.
