## Conformance

A conforming document MUST validate against the schemas published under
`/schemas/`. Producers SHOULD emit an `@context` so that documents can be
interpreted as linked data. Consumers MAY ignore properties they do not
recognise.
